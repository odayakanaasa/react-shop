query allProducts(
  $categoryId: Int
  $filterStr: String
  $sorting: String
  $offset: Int
  $first: Int
) {
  allProducts(
    categoryId: $categoryId
    filterStr: $filterStr
    sorting: $sorting
    offset: $offset
    first: $first
  ) {
    filters {
      id
      name
      isColor
      hasChecked
      help
      type
      position
      resetUrl
      values {
        id
        name
        isChecked
        filterValue
        help
        count
        position
        url
        value
      }
    }
    sorting {
      name
      url
      isChecked
    }
    products {
      id
      name
      shortDescription
      brand {
        id
        name
      }
      category {
        id
        name
      }
      images(size: SM, withColorOnly: true) {
        id
        src
        width
        height
        colorName
        colorValue
        isTitle
      }
      subProducts {
        id
        article
        price
        oldPrice
        discount
      }
    }
    found
  }
}
